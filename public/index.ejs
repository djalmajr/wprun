<!DOCTYPE html>
<html>

<head>
  <title>
    <%= htmlWebpackPlugin.options.title || 'Title'%>
  </title>

  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="<%= htmlWebpackPlugin.options.description || 'Content'%>">

  <link rel="shortcut icon" type="image/x-icon" href="favicon.ico" />

  <% if (htmlWebpackPlugin.options.stylesheets) { %>
    <% for (var stylesheet of htmlWebpackPlugin.options.stylesheets) { %>
      <link href="<%= stylesheet %>" rel="stylesheet" />
    <% } %>
  <% } %>
</head>

<body>
  <noscript>
    You must enable JavaScript to see this page correctly.
  </noscript>

  <div id="__wrapper__">
    <div class="app__loader">
      <div class="app__loader__spinner"></div>
    </div>
  </div>

  <% if (htmlWebpackPlugin.options.scripts) { %>
    <% for (var script of htmlWebpackPlugin.options.scripts) { %>
      <script type="text/javascript" src="<%= script %>"></script>
    <% } %>
  <% } %>
</body>

</html>
